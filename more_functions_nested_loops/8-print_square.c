#include "main.h"
/**
 *print_square - function that prints square
 *@size: size of square
 *
 */
void print_square(int size)
{
	int l,c;

	for (l = 0; l <= size; l++)
	{
		for (s = 0; s <= size; c++)
		{
			_putchar(35);
		}
	}
}
